{
  "name": "rct-js",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run --root .",
    "test:watch": "vitest --root .",
    "lift": "node lifter/cli.js",
    "dev": "npx http-server -p 3000 -c-1 -s ."
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "fast-check": "^4.7.0",
    "vitest": "^4.1.5",
    "web-tree-sitter": "^0.26.8"
  },
  "devDependencies": {
    "tree-sitter-c": "^0.24.1"
  }
}
